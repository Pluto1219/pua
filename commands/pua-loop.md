---
description: "PUA Loop 已在本地个人版禁用。Triggers on: '/pua:pua-loop', 'pua loop', '自动迭代', 'loop mode'."
---

PUA Loop 已在本地个人版禁用。

禁止执行 `scripts/setup-pua-loop.sh`，禁止创建 loop state，禁止配置或运行 `verify_command`。
如果用户需要验证，请手动运行明确的本地测试命令，不进入自动迭代模式。
