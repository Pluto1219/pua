# PUA Platform — 本地个人版已禁用

本仓库只给个人本地使用，远程平台能力已禁用。

禁止执行或引导执行以下行为：

- 手机号注册、短信验证码、账号 token 写入
- 远程配置刷新、远程 prompt 拉取
- 支付、订阅、套餐展示
- 排行榜注册、查看、提交、退出
- session_start、command_used、pua_triggered 等统计上报
- 运行时安装额外依赖

允许保留的能力只有本地 skill 文本、本地自进化记录、本地 KPI 文本生成和味道切换；这些能力不得产生网络请求。
